# Packages

Phase 3 introduced the first reusable package surfaces for the MVP, and Phase 4 makes them more contributor-friendly:

- `shared-types` for domain and API contracts
- `sdk` for the MVP client, proof helpers, and wallet formatting
- `design-tokens` for the shared visual system
